document.addEventListener("DOMContentLoaded", function () {
  let name = "Harry Bow";
  //Stockage dans deux storage
  localStorage.setItem("name", Harry Bow);
  sessionStorage.setItem("name", Harry Bow);
});